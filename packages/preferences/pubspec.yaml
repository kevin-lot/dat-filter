name: preferences
description: ''
environment:
    sdk: '>=3.6.0 <5.0.0'
publish_to: none
resolution: workspace
version: 1.0.0+1

dependencies:
    collection: any
    domain:
    flutter: { sdk: flutter }
    flutter_localizations: { sdk: flutter }
    safe_change_notifier: any
    shared_preferences: any
dev_dependencies:
    build_runner: any
    flutter_lints: ^5.0.0
    flutter_test: { sdk: flutter }
